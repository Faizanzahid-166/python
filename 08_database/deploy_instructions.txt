// add these to deploy the project to render build and start command
pip install -r requirements.txt

when u used root and start
08_database/todoList
cd todoList && gunicorn todoList.wsgi
web: gunicorn todoList.wsgi

when u used root and start
08_database
gunicorn todoList.wsgi:application



// requirements.txt
pip freeze > requirements.txt 

// what should we install
pip install dj-database-url   
